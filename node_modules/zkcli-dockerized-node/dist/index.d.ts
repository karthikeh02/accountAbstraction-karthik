import { ModuleNode } from "zksync-cli/lib";
import type { ConfigHandler } from "zksync-cli/lib";
export default class SetupModule extends ModuleNode {
    constructor(config: ConfigHandler);
    gitUrl: string;
    get gitFolder(): string;
    get composeFile(): string;
    get nodeInfo(): {
        id: number;
        name: string;
        network: string;
        rpcUrl: string;
        l1Chain: {
            id: number;
            name: string;
            network: string;
            rpcUrl: string;
        };
    };
    isInstalled(): Promise<boolean>;
    waitForContractsDeployment(): Promise<void>;
    install(): Promise<void>;
    isRunning(): Promise<boolean>;
    start(): Promise<void>;
    getStartupInfo(): (string | {
        text: string;
        list: string[];
    })[];
    getLogs(): Promise<string[]>;
    update(): Promise<void>;
    stop(): Promise<void>;
    clean(): Promise<void>;
}
