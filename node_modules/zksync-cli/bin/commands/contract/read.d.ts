import type { DefaultTransactionOptions } from "../../common/options.js";
import type { Command } from "commander";
type CallOptions = DefaultTransactionOptions & {
    contract?: string;
    method?: string;
    arguments?: string[];
    data?: string;
    outputTypes: string[];
    from?: string;
    abi?: string;
    decodeSkip?: boolean;
    showInfo?: boolean;
};
export declare const handler: (options: CallOptions, context: Command) => Promise<void>;
export {};
