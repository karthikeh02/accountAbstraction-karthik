import type { DefaultTransactionOptions } from "../../common/options.js";
import type { Command } from "commander";
type WriteOptions = DefaultTransactionOptions & {
    contract?: string;
    method?: string;
    arguments?: string[];
    value?: string;
    data?: string;
    abi?: string;
    showInfo?: boolean;
};
export declare const handler: (options: WriteOptions, context: Command) => Promise<void>;
export {};
