import type { DefaultOptions } from "../../common/options.js";
type BalanceOptions = DefaultOptions & {
    chain?: string;
    rpc?: string;
    address?: string;
    token?: string;
};
export declare const handler: (options: BalanceOptions) => Promise<void>;
export {};
